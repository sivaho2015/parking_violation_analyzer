package processor;

/**
 * This interface declares a method that calculates the average livable area or market value in a ZIP code area.
 * @author sivah
 *
 */
public interface AverageProcessor {
	
	double getAverageData(String ZIPCode);

}
